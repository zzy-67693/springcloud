server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/


    # 客户端端降级配置
    feign:
      hystrix:
        enabled: true
